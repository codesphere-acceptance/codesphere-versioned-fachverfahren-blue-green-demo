# Intentionally minimal.
#
# Do NOT copy the learning platform's .npmrc. It pins @apiland to a private
# GitLab registry, which breaks `pnpm install` on any machine without
# credentials for it. demo-app uses no private packages.
